server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
#    url: jdbc:mysql://user-mysql:3306/user?characterEncoding=utf8&useSSL=false
    url: jdbc:mysql://withme-mysql:3306/withme?characterEncoding=utf8&useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true